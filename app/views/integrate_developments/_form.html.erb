<%= form_for(@integrate_development) do |f| %>
  <% if @integrate_development.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@integrate_development.errors.count, "error") %> prohibited this integrate_development from being saved:</h2>

      <ul>
      <% @integrate_development.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
      <%= f.label :tag_list, "Tags (separated by commas)" %><br />
        <%= f.text_field :tag_list %>
      </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
